#:kivy 1.10.1
#:import math math
#:include bouncingimage.kv

<OverlappingImage>:
    keep_data: True

<LoopingImage>:
    pos: self.x, self.y
    source: 'Images\\chars\\{}\\{}_idle_anim_f{}.png'.format(self.image_name, self.image_name, self.image_num)
    texture: self.texture

<Character>:
    image_name: "big_zombie"
    image_state: "idle"
    source: 'Images\\chars\\{}\\{}_{}_anim_f{}.png'.format(self.image_name, self.image_name, self.image_state, self.image_num)
    width: self.width
    size_hint_y: None
    allow_stretch: True
    keep_ratio: True

<MonsterCafe>:
    player: monster
    # otherMonsterImage: otherMonster
    # ootherMonsterImage: ootherMonster
    storeFloor: sFloor
    
    Floor:
        id: sFloor
        width: self.parent.width
        height: self.parent.height
        pos_hint: 0,0

    # NonPlayerCharacter:
    #     id: otherMonster
    #     pos: self.x, self.y
    #     right: self.parent.width / 2
    #     top: (self.parent.height/ 2) + 0.2
    #     image_name: 'big_demon'

    # NonPlayerCharacter:
    #     id: ootherMonster
    #     pos: self.x, self.y
    #     right: self.parent.width / 2
    #     top: (self.parent.height/ 2) + 5
    #     image_name: 'skelet'

    PlayerCharacter:
        id: monster
        right: self.parent.width / 2
        pos_hint: 0, 0
        image_name: 'big_demon'
        width: self.width